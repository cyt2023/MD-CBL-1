# Demo Checklist

- Run `python -m src.main`
- Confirm outputs in `outputs/results/`, `outputs/figures/`, `outputs/reports/`, and `outputs/agent_traces/`
- Show baseline, smart-priority, and agentic comparison metrics
- Show an agent trace JSON file and the trace summary CSV
- Explain how the verifier prevents invalid LLM actions from entering the simulator
- Demonstrate fallback behaviour when no API key is configured
